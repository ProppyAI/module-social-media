{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.harness.proppyai.com/integration/v1/integration.schema.json",
  "title": "HARNESS Integration Config",
  "description": "Configuration for external service connections in a deployment",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "properties": {
      "type": {
        "type": "string",
        "enum": ["mcp", "api"],
        "description": "Connection type — mcp for Model Context Protocol servers, api for direct API"
      },
      "transport": {
        "type": "string",
        "enum": ["stdio", "http", "sse"],
        "description": "MCP transport type (only for type: mcp)"
      },
      "url": {
        "type": "string",
        "description": "Server URL (for http/sse transport or api type)"
      },
      "command": {
        "type": "string",
        "description": "Command to run (for stdio transport)"
      },
      "args": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Command arguments (for stdio transport)"
      },
      "required": {
        "type": "boolean",
        "default": false,
        "description": "Whether this integration is required for the deployment to function"
      }
    },
    "required": ["type"]
  }
}
